---
import Base from "../components/Base.astro";
import { getPathPath } from "../utils/getPagePath";

const { url } = Astro.request;

const currentPage = getPathPath(url);
---

<Base activeNavLink={currentPage}>
  <>Hello World</>
</Base>
